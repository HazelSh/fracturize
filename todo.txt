---- needs to go faster! I want 100m points in realtime!
  -- "motherfucking realtime motherfucking raytracing"

---- lighting/shadowing?
 -- (how the actual fuck? well, fairlight were doing it realtime in 2010, so...)
 -- ... Fairlight/CNCD were not, actually, rendering IFS fractals.
  - this matters. what the fuck is the normal?
   - no, seriously. random bit of grit away from body of fractal. what angle is it at to the light?
    - is it a tiny ball, I guess? and the balls have a size?
     - and when there's other balls in the size, sample those, figure out a gradient...?
      - tangent to that toward open-space is the normal...? seriously, this is fucked. worse than SDF by far. 

---- ...particle system things? 'motion transforms'? velocity, acceleration, mass, per-point?

---- dynamic point density / infinite zoom

---- in-engine scene browser, super lo-res preview

---- little bit of atmosphere in the shading? darken/desat colors further from camera?
 -- depth is hard to spot at the moment
  - much easier to add a touch of fog than to figure out pointcloud shadowing
 -- unfuck existing fog implementation? 

---- saving .gifs / animated/motion png? or modern animated .avifs
 -- DONE (fable5, 2026-07-15): `--render out.avif` = animated AVIF, rav1e +
    hand-rolled avis muxer in src/avif.rs. --fps/--seconds/--quality.

---- rework keyboard shortcuts + list → into real GUI with buttons etc

---- infinite zoom plan / space-filling

---- better handling of point counts, bake into UI, remove from scene. not a scene param.

---- better handling of fizz / sparkle -- tuned lower for more stable renders

---- chaos traces not showing what I want to show...

---- camera paths with .gif render or video render?
 -- DONE (fable5, 2026-07-15): [[camera.path]] spline keypoints in scenes
    (src/path.rs — Catmull-Rom over yaw/pitch/log-distance/focus, closed
    loops, multi-turn spirals). In-app: Y add key, Ctrl+Y loop, Z preview.
    Pathless scenes animate as a seamless full orbit. scenes/winze.toml +
    renders/winze.avif are the demo.
 -- automatic feature detection, automatic camera positioning/pointing? at/lookat 'voids', 'clusters'?




🕮⊞◌⃘⁂
